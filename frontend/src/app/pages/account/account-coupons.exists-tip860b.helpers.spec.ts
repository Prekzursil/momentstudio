import { AccountCouponsComponent } from './account-coupons.component';

describe('AccountCouponsComponent tip860b',()=>{it('proto',()=>{expect(Object.create(AccountCouponsComponent.prototype)).toBeTruthy();});});
