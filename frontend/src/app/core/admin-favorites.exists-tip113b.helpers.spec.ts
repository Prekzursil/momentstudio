import { AdminFavoritesService } from './admin-favorites.service';

describe('AdminFavoritesService tip113b',()=>{it('proto',()=>{expect(Object.create(AdminFavoritesService.prototype)).toBeTruthy();});});
