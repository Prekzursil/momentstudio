import { AccountOverviewComponent } from './account-overview.component';

describe('AccountOverviewComponent tip8',()=>{it('proto',()=>{expect(Object.create(AccountOverviewComponent.prototype)).toBeTruthy();});});
