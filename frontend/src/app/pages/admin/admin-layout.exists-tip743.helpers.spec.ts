import { AdminLayoutComponent } from './admin-layout.component';

describe('AdminLayoutComponent tip743',()=>{it('proto',()=>{expect(Object.create(AdminLayoutComponent.prototype)).toBeTruthy();});});
