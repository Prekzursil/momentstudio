import { AdminFavoritesService } from './admin-favorites.service';

describe('AdminFavoritesService tip251b',()=>{it('proto',()=>{expect(Object.create(AdminFavoritesService.prototype)).toBeTruthy();});});
