import { CheckoutShippingStepComponent } from './checkout-shipping-step.component';

describe('Shipping tip',()=>{it('p',()=>{expect(Object.create(CheckoutShippingStepComponent.prototype)).toBeTruthy();});});
