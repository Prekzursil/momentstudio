import { NetopiaCancelComponent } from './netopia-cancel.component';

describe('NetopiaCancelComponent tip33',()=>{it('proto',()=>{expect(Object.create(NetopiaCancelComponent.prototype)).toBeTruthy();});});
