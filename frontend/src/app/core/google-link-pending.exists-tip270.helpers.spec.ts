import { GoogleLinkPendingService } from './google-link-pending.service';

describe('GoogleLinkPendingService tip270',()=>{it('proto',()=>{expect(Object.create(GoogleLinkPendingService.prototype)).toBeTruthy();});});
