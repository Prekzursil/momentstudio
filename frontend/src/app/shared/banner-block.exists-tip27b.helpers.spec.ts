import { BannerBlockComponent } from './banner-block.component';

describe('BannerBlockComponent tip27b',()=>{it('proto',()=>{expect(Object.create(BannerBlockComponent.prototype)).toBeTruthy();});});
