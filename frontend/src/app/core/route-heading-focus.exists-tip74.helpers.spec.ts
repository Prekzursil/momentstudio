import { RouteHeadingFocusService } from './route-heading-focus.service';

describe('RouteHeadingFocusService tip74',()=>{it('proto',()=>{expect(Object.create(RouteHeadingFocusService.prototype)).toBeTruthy();});});
