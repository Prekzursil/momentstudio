import { StructuredDataService } from './structured-data.service';

describe('StructuredDataService tip1642b',()=>{it('proto',()=>{expect(Object.create(StructuredDataService.prototype)).toBeTruthy();});});
