import { AdminInventoryComponent } from './admin-inventory.component';

describe('AdminInventoryComponent tip1182',()=>{it('proto',()=>{expect(Object.create(AdminInventoryComponent.prototype)).toBeTruthy();});});
