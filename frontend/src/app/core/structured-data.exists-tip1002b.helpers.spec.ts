import { StructuredDataService } from './structured-data.service';

describe('StructuredDataService tip1002b',()=>{it('proto',()=>{expect(Object.create(StructuredDataService.prototype)).toBeTruthy();});});
