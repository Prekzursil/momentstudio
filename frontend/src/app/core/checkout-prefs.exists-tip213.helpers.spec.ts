import { CheckoutPrefsService } from './checkout-prefs.service';

describe('CheckoutPrefsService tip213',()=>{it('proto',()=>{expect(Object.create(CheckoutPrefsService.prototype)).toBeTruthy();});});
