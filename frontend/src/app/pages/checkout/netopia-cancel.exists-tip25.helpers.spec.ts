import { NetopiaCancelComponent } from './netopia-cancel.component';

describe('NetopiaCancelComponent tip25',()=>{it('proto',()=>{expect(Object.create(NetopiaCancelComponent.prototype)).toBeTruthy();});});
