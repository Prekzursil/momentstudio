import { ChangePasswordComponent } from './change-password.component';

describe('ChangePasswordComponent tip1522',()=>{it('proto',()=>{expect(Object.create(ChangePasswordComponent.prototype)).toBeTruthy();});});
