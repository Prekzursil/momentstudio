import { LegalConsentModalComponent } from './legal-consent-modal.component';

describe('LegalConsentModalComponent tip32',()=>{it('proto',()=>{expect(Object.create(LegalConsentModalComponent.prototype)).toBeTruthy();});});
