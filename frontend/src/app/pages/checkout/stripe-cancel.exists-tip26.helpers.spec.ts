import { StripeCancelComponent } from './stripe-cancel.component';

describe('StripeCancelComponent tip26',()=>{it('proto',()=>{expect(Object.create(StripeCancelComponent.prototype)).toBeTruthy();});});
