import { AccountOrdersComponent } from './account-orders.component';

describe('AccountOrdersComponent tip743',()=>{it('proto',()=>{expect(Object.create(AccountOrdersComponent.prototype)).toBeTruthy();});});
