import { CheckoutPaymentStepComponent } from './checkout-payment-step.component';

describe('CheckoutPayment tip',()=>{it('proto',()=>{expect(Object.create(CheckoutPaymentStepComponent.prototype)).toBeTruthy();});});
