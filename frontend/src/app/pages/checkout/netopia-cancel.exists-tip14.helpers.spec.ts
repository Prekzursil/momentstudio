import { NetopiaCancelComponent } from './netopia-cancel.component';

describe('NetopiaCancelComponent tip14',()=>{it('proto',()=>{expect(Object.create(NetopiaCancelComponent.prototype)).toBeTruthy();});});
