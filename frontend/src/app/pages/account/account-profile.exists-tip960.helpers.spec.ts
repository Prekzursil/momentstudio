import { AccountProfileComponent } from './account-profile.component';

describe('AccountProfileComponent tip960',()=>{it('proto',()=>{expect(Object.create(AccountProfileComponent.prototype)).toBeTruthy();});});
