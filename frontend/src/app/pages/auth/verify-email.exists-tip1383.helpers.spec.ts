import { VerifyEmailComponent } from './verify-email.component';

describe('VerifyEmailComponent tip1383',()=>{it('proto',()=>{expect(Object.create(VerifyEmailComponent.prototype)).toBeTruthy();});});
