import { ProductImageManagerModalComponent } from './product-image-manager-modal.component';

describe('ProductImageManagerModalComponent tip38',()=>{it('proto',()=>{expect(Object.create(ProductImageManagerModalComponent.prototype)).toBeTruthy();});});
