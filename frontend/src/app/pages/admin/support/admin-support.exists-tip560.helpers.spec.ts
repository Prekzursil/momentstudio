import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip560',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
