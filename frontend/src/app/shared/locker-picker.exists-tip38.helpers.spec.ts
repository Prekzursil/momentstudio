import { LockerPickerComponent } from './locker-picker.component';

describe('LockerPickerComponent tip38',()=>{it('proto',()=>{expect(Object.create(LockerPickerComponent.prototype)).toBeTruthy();});});
