import { LocalizedTextEditorComponent } from './localized-text-editor.component';

describe('LocalizedTextEditorComponent tip1124',()=>{it('proto',()=>{expect(Object.create(LocalizedTextEditorComponent.prototype)).toBeTruthy();});});
