import { NewsletterUnsubscribeComponent } from './newsletter-unsubscribe.component';

describe('NewsletterUnsubscribeComponent tip14',()=>{it('proto',()=>{expect(Object.create(NewsletterUnsubscribeComponent.prototype)).toBeTruthy();});});
