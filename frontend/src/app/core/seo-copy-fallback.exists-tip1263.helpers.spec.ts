import { SeoCopyFallbackService } from './seo-copy-fallback.service';

describe('SeoCopyFallbackService tip1263',()=>{it('proto',()=>{expect(Object.create(SeoCopyFallbackService.prototype)).toBeTruthy();});});
