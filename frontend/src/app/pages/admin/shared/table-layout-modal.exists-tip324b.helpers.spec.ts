import { TableLayoutModalComponent } from './table-layout-modal.component';

describe('TableLayoutModalComponent tip324b',()=>{it('proto',()=>{expect(Object.create(TableLayoutModalComponent.prototype)).toBeTruthy();});});
