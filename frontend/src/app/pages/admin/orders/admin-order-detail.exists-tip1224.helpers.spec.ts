import { AdminOrderDetailComponent } from './admin-order-detail.component';

describe('AdminOrderDetailComponent tip1224',()=>{it('proto',()=>{expect(Object.create(AdminOrderDetailComponent.prototype)).toBeTruthy();});});
