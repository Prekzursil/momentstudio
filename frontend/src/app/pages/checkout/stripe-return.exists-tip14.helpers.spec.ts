import { StripeReturnComponent } from './stripe-return.component';

describe('StripeReturnComponent tip14',()=>{it('proto',()=>{expect(Object.create(StripeReturnComponent.prototype)).toBeTruthy();});});
