import { StripeCancelComponent } from './stripe-cancel.component';

describe('StripeCancelComponent tip',()=>{it('proto',()=>{expect(Object.create(StripeCancelComponent.prototype)).toBeTruthy();});});
