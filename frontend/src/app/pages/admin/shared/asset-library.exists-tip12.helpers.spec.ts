import { AssetLibraryComponent } from './asset-library.component';

describe('AssetLibraryComponent tip12',()=>{it('proto',()=>{expect(Object.create(AssetLibraryComponent.prototype)).toBeTruthy();});});
