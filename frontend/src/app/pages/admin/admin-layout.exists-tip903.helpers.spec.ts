import { AdminLayoutComponent } from './admin-layout.component';

describe('AdminLayoutComponent tip903',()=>{it('proto',()=>{expect(Object.create(AdminLayoutComponent.prototype)).toBeTruthy();});});
