name: Quality Zero Gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  aggregate-gate:
    uses: Prekzursil/quality-zero-platform/.github/workflows/reusable-quality-zero-gate.yml@37a6db2c75dc6891eea39007c96e8f9cbde22579 # main
    with:
      repo_slug: ${{ github.repository }}
      event_name: ${{ github.event_name }}
      sha: ${{ github.event.pull_request.head.sha || github.sha }}
    secrets: inherit
