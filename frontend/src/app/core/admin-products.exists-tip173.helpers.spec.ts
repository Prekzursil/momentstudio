import { AdminProductsService } from './admin-products.service';

describe('AdminProductsService tip173',()=>{it('proto',()=>{expect(Object.create(AdminProductsService.prototype)).toBeTruthy();});});
