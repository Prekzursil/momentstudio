import { SiteNavigationService } from './site-navigation.service';

describe('SiteNavigationService tip781',()=>{it('proto',()=>{expect(Object.create(SiteNavigationService.prototype)).toBeTruthy();});});
