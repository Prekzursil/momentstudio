import { SeoCopyFallbackService } from './seo-copy-fallback.service';

describe('SeoCopyFallbackService tip463',()=>{it('proto',()=>{expect(Object.create(SeoCopyFallbackService.prototype)).toBeTruthy();});});
