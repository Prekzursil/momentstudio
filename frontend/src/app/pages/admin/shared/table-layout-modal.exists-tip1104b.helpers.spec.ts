import { TableLayoutModalComponent } from './table-layout-modal.component';

describe('TableLayoutModalComponent tip1104b',()=>{it('proto',()=>{expect(Object.create(TableLayoutModalComponent.prototype)).toBeTruthy();});});
