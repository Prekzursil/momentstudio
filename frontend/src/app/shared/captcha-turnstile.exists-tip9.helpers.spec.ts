import { CaptchaTurnstileComponent } from './captcha-turnstile.component';

describe('CaptchaTurnstileComponent tip9',()=>{it('proto',()=>{expect(Object.create(CaptchaTurnstileComponent.prototype)).toBeTruthy();});});
