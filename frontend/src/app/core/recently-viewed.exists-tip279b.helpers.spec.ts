import { RecentlyViewedService } from './recently-viewed.service';

describe('RecentlyViewedService tip279b',()=>{it('proto',()=>{expect(Object.create(RecentlyViewedService.prototype)).toBeTruthy();});});
