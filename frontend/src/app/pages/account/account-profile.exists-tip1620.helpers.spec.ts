import { AccountProfileComponent } from './account-profile.component';

describe('AccountProfileComponent tip1620',()=>{it('proto',()=>{expect(Object.create(AccountProfileComponent.prototype)).toBeTruthy();});});
