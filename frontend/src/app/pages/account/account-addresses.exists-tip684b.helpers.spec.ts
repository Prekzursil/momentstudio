import { AccountAddressesComponent } from './account-addresses.component';

describe('AccountAddressesComponent tip684b',()=>{it('proto',()=>{expect(Object.create(AccountAddressesComponent.prototype)).toBeTruthy();});});
