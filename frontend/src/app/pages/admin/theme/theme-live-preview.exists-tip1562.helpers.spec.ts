import { ThemeLivePreviewComponent } from './theme-live-preview.component';

describe('ThemeLivePreviewComponent tip1562',()=>{it('proto',()=>{expect(Object.create(ThemeLivePreviewComponent.prototype)).toBeTruthy();});});
