import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip1380',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
