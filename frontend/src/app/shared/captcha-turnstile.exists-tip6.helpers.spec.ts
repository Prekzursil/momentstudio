import { CaptchaTurnstileComponent } from './captcha-turnstile.component';

describe('CaptchaTurnstileComponent tip6',()=>{it('proto',()=>{expect(Object.create(CaptchaTurnstileComponent.prototype)).toBeTruthy();});});
