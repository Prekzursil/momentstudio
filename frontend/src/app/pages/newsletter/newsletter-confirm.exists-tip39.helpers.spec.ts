import { NewsletterConfirmComponent } from './newsletter-confirm.component';

describe('NewsletterConfirmComponent tip39',()=>{it('proto',()=>{expect(Object.create(NewsletterConfirmComponent.prototype)).toBeTruthy();});});
