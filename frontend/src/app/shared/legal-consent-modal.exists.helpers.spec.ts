import { LegalConsentModalComponent } from './legal-consent-modal.component';

describe('LegalConsentModalComponent tip',()=>{it('proto',()=>{expect(Object.create(LegalConsentModalComponent.prototype)).toBeTruthy();});});
