import { NotificationsService } from './notifications.service';

describe('NotificationsService tip1144',()=>{it('proto',()=>{expect(Object.create(NotificationsService.prototype)).toBeTruthy();});});
