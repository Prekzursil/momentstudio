import { CheckoutPrefsService } from './checkout-prefs.service';

describe('CheckoutPrefsService tip842',()=>{it('proto',()=>{expect(Object.create(CheckoutPrefsService.prototype)).toBeTruthy();});});
