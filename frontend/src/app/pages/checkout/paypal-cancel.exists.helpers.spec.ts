import { PayPalCancelComponent } from './paypal-cancel.component';

describe('PayPalCancelComponent tip',()=>{it('proto',()=>{expect(Object.create(PayPalCancelComponent.prototype)).toBeTruthy();});});
