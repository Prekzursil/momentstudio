import { RecentlyViewedService } from './recently-viewed.service';

describe('RecentlyViewedService tip187b',()=>{it('proto',()=>{expect(Object.create(RecentlyViewedService.prototype)).toBeTruthy();});});
