import { PayPalCancelComponent } from './paypal-cancel.component';

describe('PayPalCancelComponent tip40',()=>{it('proto',()=>{expect(Object.create(PayPalCancelComponent.prototype)).toBeTruthy();});});
