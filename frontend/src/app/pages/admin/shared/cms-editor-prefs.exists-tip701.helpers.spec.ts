import { CmsEditorPrefsService } from './cms-editor-prefs.service';

describe('CmsEditorPrefsService tip701',()=>{it('proto',()=>{expect(Object.create(CmsEditorPrefsService.prototype)).toBeTruthy();});});
