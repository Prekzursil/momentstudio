# Frontend runtime configuration
#
# If you're using the dev proxy (npm start / start.sh), keep API_BASE_URL as `/api/v1`
# so requests like `/api/v1/catalog/products` are proxied to the backend.
#
# If you want to call the backend directly without a proxy, set a full URL like:
# API_BASE_URL=http://localhost:8000/api/v1

API_BASE_URL=/api/v1

# One of: development | production
APP_ENV=development

# Optional app version/build identifier for Sentry release tracking
APP_VERSION=

# Optional Stripe support for checkout (redirect-based).
# Requires backend Stripe credentials too (STRIPE_SECRET_KEY_*).
STRIPE_ENABLED=

# Optional PayPal support for checkout (redirect-based).
# Requires backend PayPal credentials too (PAYPAL_CLIENT_ID / PAYPAL_CLIENT_SECRET).
PAYPAL_ENABLED=

# Optional Netopia support for checkout (redirect-based).
# Enable only after backend Netopia credentials are configured.
NETOPIA_ENABLED=

# Optional address autocomplete in account address form (external provider).
# Keep disabled in production unless you've reviewed provider terms/limits.
ADDRESS_AUTOCOMPLETE_ENABLED=

# Optional Microsoft Clarity project id (public storefront only, anonymous sessions only).
FRONTEND_CLARITY_PROJECT_ID=
# Enable Clarity bootstrap when project id is provided.
CLARITY_ENABLED=1

# Optional Sentry DSN for frontend error reporting
SENTRY_DSN=
# Frontend Sentry master switch (still requires SENTRY_DSN to actually initialize).
SENTRY_ENABLED=1
# Send default PII to Sentry (requested global policy for this repository).
SENTRY_SEND_DEFAULT_PII=1
# Frontend Sentry tracing sample rate (0.0-1.0).
SENTRY_TRACES_SAMPLE_RATE=1.0
# Frontend Sentry replay sampling for all sessions (0.0-1.0).
SENTRY_REPLAY_SESSION_SAMPLE_RATE=0.25
# Frontend Sentry replay sampling for sessions with errors (0.0-1.0).
SENTRY_REPLAY_ON_ERROR_SAMPLE_RATE=1.0

# Optional Cloudflare Turnstile site key for CAPTCHA (auth + blog comments).
# Set up a Turnstile widget and allow localhost during development.
CAPTCHA_SITE_KEY=

# FX rates are fetched from the backend (/api/v1/fx/rates) for EN display-only
# approximations (checkout/pricing remains in RON).
