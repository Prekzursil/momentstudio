import { AdminContentSchedulingComponent } from './admin-content-scheduling.component';

describe('AdminContentSchedulingComponent tip420',()=>{it('proto',()=>{expect(Object.create(AdminContentSchedulingComponent.prototype)).toBeTruthy();});});
