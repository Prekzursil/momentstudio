import { GoogleLinkPendingService } from './google-link-pending.service';

describe('GoogleLinkPendingService tip304b',()=>{it('proto',()=>{expect(Object.create(GoogleLinkPendingService.prototype)).toBeTruthy();});});
