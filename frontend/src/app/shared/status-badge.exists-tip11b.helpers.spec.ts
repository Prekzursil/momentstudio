import { StatusBadgeComponent } from './status-badge.component';

describe('StatusBadgeComponent tip11b',()=>{it('proto',()=>{expect(Object.create(StatusBadgeComponent.prototype)).toBeTruthy();});});
