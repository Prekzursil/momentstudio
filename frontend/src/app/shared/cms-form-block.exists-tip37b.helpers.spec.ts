import { CmsFormBlockComponent } from './cms-form-block.component';

describe('CmsFormBlockComponent tip37b',()=>{it('proto',()=>{expect(Object.create(CmsFormBlockComponent.prototype)).toBeTruthy();});});
