import { StripeCancelComponent } from './stripe-cancel.component';

describe('StripeCancelComponent tip44',()=>{it('proto',()=>{expect(Object.create(StripeCancelComponent.prototype)).toBeTruthy();});});
