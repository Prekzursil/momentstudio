import { AdminProductsService } from './admin-products.service';

describe('AdminProductsService tip211',()=>{it('proto',()=>{expect(Object.create(AdminProductsService.prototype)).toBeTruthy();});});
