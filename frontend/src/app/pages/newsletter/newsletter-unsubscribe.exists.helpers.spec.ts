import { NewsletterUnsubscribeComponent } from './newsletter-unsubscribe.component';

describe('NewsletterUnsubscribeComponent tip',()=>{it('proto',()=>{expect(Object.create(NewsletterUnsubscribeComponent.prototype)).toBeTruthy();});});
