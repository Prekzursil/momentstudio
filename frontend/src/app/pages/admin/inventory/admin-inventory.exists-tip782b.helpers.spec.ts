import { AdminInventoryComponent } from './admin-inventory.component';

describe('AdminInventoryComponent tip782b',()=>{it('proto',()=>{expect(Object.create(AdminInventoryComponent.prototype)).toBeTruthy();});});
