import { AddressFormComponent } from './address-form.component';

describe('AddressFormComponent tip43b',()=>{it('proto',()=>{expect(Object.create(AddressFormComponent.prototype)).toBeTruthy();});});
