import { PayPalCancelComponent } from './paypal-cancel.component';

describe('PayPalCancelComponent tip14',()=>{it('proto',()=>{expect(Object.create(PayPalCancelComponent.prototype)).toBeTruthy();});});
