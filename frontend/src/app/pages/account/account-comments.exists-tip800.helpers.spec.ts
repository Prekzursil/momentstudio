import { AccountCommentsComponent } from './account-comments.component';

describe('AccountCommentsComponent tip800',()=>{it('proto',()=>{expect(Object.create(AccountCommentsComponent.prototype)).toBeTruthy();});});
