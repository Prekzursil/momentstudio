import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip960',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
