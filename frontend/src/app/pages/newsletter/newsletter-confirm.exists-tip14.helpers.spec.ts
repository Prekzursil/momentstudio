import { NewsletterConfirmComponent } from './newsletter-confirm.component';

describe('NewsletterConfirmComponent tip14',()=>{it('proto',()=>{expect(Object.create(NewsletterConfirmComponent.prototype)).toBeTruthy();});});
