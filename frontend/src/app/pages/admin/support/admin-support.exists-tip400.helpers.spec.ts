import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip400',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
