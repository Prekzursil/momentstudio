import { CmsFormBlockComponent } from './cms-form-block.component';

describe('CmsFormBlockComponent tip7',()=>{it('proto',()=>{expect(Object.create(CmsFormBlockComponent.prototype)).toBeTruthy();});});
