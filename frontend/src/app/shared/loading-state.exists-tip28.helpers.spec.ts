import { LoadingStateComponent } from './loading-state.component';

describe('LoadingStateComponent tip28',()=>{it('proto',()=>{expect(Object.create(LoadingStateComponent.prototype)).toBeTruthy();});});
