import { CheckoutReturnErrorCardComponent } from './checkout-return-error-card.component';

describe('ReturnErr tip',()=>{it('p',()=>{expect(Object.create(CheckoutReturnErrorCardComponent.prototype)).toBeTruthy();});});
