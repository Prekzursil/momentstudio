import { ChangePasswordComponent } from './change-password.component';

describe('ChangePasswordComponent tip1162',()=>{it('proto',()=>{expect(Object.create(ChangePasswordComponent.prototype)).toBeTruthy();});});
