import { AccountCommentsComponent } from './account-comments.component';

describe('AccountCommentsComponent tip560',()=>{it('proto',()=>{expect(Object.create(AccountCommentsComponent.prototype)).toBeTruthy();});});
