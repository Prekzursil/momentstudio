import { ThemeSegmentedControlComponent } from './theme-segmented-control.component';

describe('ThemeSegmentedControlComponent tip39',()=>{it('proto',()=>{expect(Object.create(ThemeSegmentedControlComponent.prototype)).toBeTruthy();});});
