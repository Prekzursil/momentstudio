import { SeoCopyFallbackService } from './seo-copy-fallback.service';

describe('SeoCopyFallbackService tip41b',()=>{it('proto',()=>{expect(Object.create(SeoCopyFallbackService.prototype)).toBeTruthy();});});
