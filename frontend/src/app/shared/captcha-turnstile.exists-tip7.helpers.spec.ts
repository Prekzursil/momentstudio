import { CaptchaTurnstileComponent } from './captcha-turnstile.component';

describe('CaptchaTurnstileComponent tip7',()=>{it('proto',()=>{expect(Object.create(CaptchaTurnstileComponent.prototype)).toBeTruthy();});});
