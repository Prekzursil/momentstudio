import { AccountWishlistComponent } from './account-wishlist.component';

describe('AccountWishlistComponent tip1121',()=>{it('proto',()=>{expect(Object.create(AccountWishlistComponent.prototype)).toBeTruthy();});});
