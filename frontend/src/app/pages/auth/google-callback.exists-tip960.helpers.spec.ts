import { GoogleCallbackComponent } from './google-callback.component';

describe('GoogleCallbackComponent tip960',()=>{it('proto',()=>{expect(Object.create(GoogleCallbackComponent.prototype)).toBeTruthy();});});
