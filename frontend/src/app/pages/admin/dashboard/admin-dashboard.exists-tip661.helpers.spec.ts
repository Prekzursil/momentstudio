import { AdminDashboardComponent } from './admin-dashboard.component';

describe('AdminDashboardComponent tip661',()=>{it('proto',()=>{expect(Object.create(AdminDashboardComponent.prototype)).toBeTruthy();});});
