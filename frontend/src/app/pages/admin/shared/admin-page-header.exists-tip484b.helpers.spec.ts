import { AdminPageHeaderComponent } from './admin-page-header.component';

describe('AdminPageHeaderComponent tip484b',()=>{it('proto',()=>{expect(Object.create(AdminPageHeaderComponent.prototype)).toBeTruthy();});});
