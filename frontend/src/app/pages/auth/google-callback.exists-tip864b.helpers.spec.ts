import { GoogleCallbackComponent } from './google-callback.component';

describe('GoogleCallbackComponent tip864b',()=>{it('proto',()=>{expect(Object.create(GoogleCallbackComponent.prototype)).toBeTruthy();});});
