import { SiteNavigationService } from './site-navigation.service';

describe('SiteNavigationService tip981',()=>{it('proto',()=>{expect(Object.create(SiteNavigationService.prototype)).toBeTruthy();});});
