# Repo policy phase 2 evaluation

- Generated at: `2026-02-15T20:48:47+00:00`
- Repository: `Prekzursil/momentstudio`
- Base branch: `main`
- Window: last `60` days

## Metrics

- Merged PRs (all authors): **91**
- Merged PRs (non-bot): **86**
- Unique non-bot contributors: **1**
- Contributors with >= `2` merged PRs: **1**

## Trigger policy

To enable `1` required approval, all must be true:

1. At least `3` non-bot contributors each with >= `2` merged PRs in `60` days.
2. At least `25` non-bot merged PRs in `60` days.
3. Condition must hold for **2 consecutive monthly evaluations**.

Result: **HOLD**  
Trigger not met. Keep checks-only branch protection.

## Top non-bot contributors in window

| Login | Merged PRs |
| --- | ---: |
| `Prekzursil` | 86 |
