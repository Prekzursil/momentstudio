import { CmsEditorPrefsService } from './cms-editor-prefs.service';

describe('CmsEditorPrefsService tip581',()=>{it('proto',()=>{expect(Object.create(CmsEditorPrefsService.prototype)).toBeTruthy();});});
