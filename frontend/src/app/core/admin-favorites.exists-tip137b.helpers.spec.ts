import { AdminFavoritesService } from './admin-favorites.service';

describe('AdminFavoritesService tip137b',()=>{it('proto',()=>{expect(Object.create(AdminFavoritesService.prototype)).toBeTruthy();});});
