import { AssetLibraryComponent } from './asset-library.component';

describe('AssetLibraryComponent tip940',()=>{it('proto',()=>{expect(Object.create(AssetLibraryComponent.prototype)).toBeTruthy();});});
