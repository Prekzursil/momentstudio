import { SeoCopyFallbackService } from './seo-copy-fallback.service';

describe('SeoCopyFallbackService tip483',()=>{it('proto',()=>{expect(Object.create(SeoCopyFallbackService.prototype)).toBeTruthy();});});
