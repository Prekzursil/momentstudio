import { CheckoutPromoStepComponent } from './checkout-promo-step.component';

describe('CheckoutPromoStepComponent tip40',()=>{it('proto',()=>{expect(Object.create(CheckoutPromoStepComponent.prototype)).toBeTruthy();});});
