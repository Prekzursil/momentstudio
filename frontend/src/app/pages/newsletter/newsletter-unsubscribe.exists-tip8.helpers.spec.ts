import { NewsletterUnsubscribeComponent } from './newsletter-unsubscribe.component';

describe('NewsletterUnsubscribeComponent tip8',()=>{it('proto',()=>{expect(Object.create(NewsletterUnsubscribeComponent.prototype)).toBeTruthy();});});
