import { AccountCommentsComponent } from './account-comments.component';

describe('AccountCommentsComponent tip700',()=>{it('proto',()=>{expect(Object.create(AccountCommentsComponent.prototype)).toBeTruthy();});});
