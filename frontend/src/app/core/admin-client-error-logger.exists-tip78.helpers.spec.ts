import { AdminClientErrorLoggerService } from './admin-client-error-logger.service';

describe('AdminClientErrorLoggerService tip78',()=>{it('proto',()=>{expect(Object.create(AdminClientErrorLoggerService.prototype)).toBeTruthy();});});
