import { CarouselBlockComponent } from './carousel-block.component';

describe('CarouselBlockComponent tip7',()=>{it('proto',()=>{expect(Object.create(CarouselBlockComponent.prototype)).toBeTruthy();});});
