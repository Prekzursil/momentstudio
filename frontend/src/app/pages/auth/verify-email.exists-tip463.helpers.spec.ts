import { VerifyEmailComponent } from './verify-email.component';

describe('VerifyEmailComponent tip463',()=>{it('proto',()=>{expect(Object.create(VerifyEmailComponent.prototype)).toBeTruthy();});});
