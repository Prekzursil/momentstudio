import { AdminOrdersComponent } from './admin-orders.component';

describe('AdminOrdersComponent tip761',()=>{it('proto',()=>{expect(Object.create(AdminOrdersComponent.prototype)).toBeTruthy();});});
