import { AccountNotificationsInboxComponent } from './account-notifications-inbox.component';

describe('AccountNotificationsInboxComponent tip1041',()=>{it('proto',()=>{expect(Object.create(AccountNotificationsInboxComponent.prototype)).toBeTruthy();});});
