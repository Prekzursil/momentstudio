import { StorefrontAdminModeService } from './storefront-admin-mode.service';

describe('StorefrontAdminModeService tip783',()=>{it('proto',()=>{expect(Object.create(StorefrontAdminModeService.prototype)).toBeTruthy();});});
