import { AdminContentSchedulingComponent } from './admin-content-scheduling.component';

describe('AdminContentSchedulingComponent tip580',()=>{it('proto',()=>{expect(Object.create(AdminContentSchedulingComponent.prototype)).toBeTruthy();});});
