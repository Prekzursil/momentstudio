import { AdminLayoutComponent } from './admin-layout.component';

describe('AdminLayoutComponent tip1382b',()=>{it('proto',()=>{expect(Object.create(AdminLayoutComponent.prototype)).toBeTruthy();});});
