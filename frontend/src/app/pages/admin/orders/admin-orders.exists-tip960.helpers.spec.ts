import { AdminOrdersComponent } from './admin-orders.component';

describe('AdminOrdersComponent tip960',()=>{it('proto',()=>{expect(Object.create(AdminOrdersComponent.prototype)).toBeTruthy();});});
