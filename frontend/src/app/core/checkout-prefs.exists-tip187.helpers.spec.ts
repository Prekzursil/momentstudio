import { CheckoutPrefsService } from './checkout-prefs.service';

describe('CheckoutPrefsService tip187',()=>{it('proto',()=>{expect(Object.create(CheckoutPrefsService.prototype)).toBeTruthy();});});
