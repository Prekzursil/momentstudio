import { ThemeLivePreviewComponent } from './theme-live-preview.component';

describe('ThemeLivePreviewComponent tip522',()=>{it('proto',()=>{expect(Object.create(ThemeLivePreviewComponent.prototype)).toBeTruthy();});});
