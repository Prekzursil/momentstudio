import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip640',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
