import { AdminCouponsComponent } from './admin-coupons.component';

describe('AdminCouponsComponent tip1601',()=>{it('proto',()=>{expect(Object.create(AdminCouponsComponent.prototype)).toBeTruthy();});});
