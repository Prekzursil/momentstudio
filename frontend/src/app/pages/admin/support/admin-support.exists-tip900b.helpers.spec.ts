import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip900b',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
