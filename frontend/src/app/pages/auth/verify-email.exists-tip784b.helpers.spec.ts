import { VerifyEmailComponent } from './verify-email.component';

describe('VerifyEmailComponent tip784b',()=>{it('proto',()=>{expect(Object.create(VerifyEmailComponent.prototype)).toBeTruthy();});});
