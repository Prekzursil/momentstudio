import { AdminIpBypassComponent } from './admin-ip-bypass.component';

describe('AdminIpBypassComponent tip1562b',()=>{it('proto',()=>{expect(Object.create(AdminIpBypassComponent.prototype)).toBeTruthy();});});
