import { AccountCouponsComponent } from './account-coupons.component';

describe('AccountCouponsComponent tip1060b',()=>{it('proto',()=>{expect(Object.create(AccountCouponsComponent.prototype)).toBeTruthy();});});
