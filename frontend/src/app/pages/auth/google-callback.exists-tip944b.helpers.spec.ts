import { GoogleCallbackComponent } from './google-callback.component';

describe('GoogleCallbackComponent tip944b',()=>{it('proto',()=>{expect(Object.create(GoogleCallbackComponent.prototype)).toBeTruthy();});});
