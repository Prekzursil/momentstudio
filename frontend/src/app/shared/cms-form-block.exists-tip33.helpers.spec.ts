import { CmsFormBlockComponent } from './cms-form-block.component';

describe('CmsFormBlockComponent tip33',()=>{it('proto',()=>{expect(Object.create(CmsFormBlockComponent.prototype)).toBeTruthy();});});
