import { CmsEditorPrefsService } from './cms-editor-prefs.service';

describe('CmsEditorPrefsService tip901',()=>{it('proto',()=>{expect(Object.create(CmsEditorPrefsService.prototype)).toBeTruthy();});});
