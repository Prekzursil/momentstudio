import { NotificationsService } from './notifications.service';

describe('NotificationsService tip1124',()=>{it('proto',()=>{expect(Object.create(NotificationsService.prototype)).toBeTruthy();});});
