import { ThemeSegmentedControlComponent } from './theme-segmented-control.component';

describe('ThemeSegmentedControlComponent tip34',()=>{it('proto',()=>{expect(Object.create(ThemeSegmentedControlComponent.prototype)).toBeTruthy();});});
