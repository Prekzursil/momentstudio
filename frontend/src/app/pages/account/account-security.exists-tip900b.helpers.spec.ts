import { AccountSecurityComponent } from './account-security.component';

describe('AccountSecurityComponent tip900b',()=>{it('proto',()=>{expect(Object.create(AccountSecurityComponent.prototype)).toBeTruthy();});});
