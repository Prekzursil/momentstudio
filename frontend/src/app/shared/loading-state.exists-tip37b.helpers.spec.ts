import { LoadingStateComponent } from './loading-state.component';

describe('LoadingStateComponent tip37b',()=>{it('proto',()=>{expect(Object.create(LoadingStateComponent.prototype)).toBeTruthy();});});
