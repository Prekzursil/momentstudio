import { AdminFavoritesService } from './admin-favorites.service';

describe('AdminFavoritesService tip147b',()=>{it('proto',()=>{expect(Object.create(AdminFavoritesService.prototype)).toBeTruthy();});});
