import { StructuredDataService } from './structured-data.service';

describe('StructuredDataService tip642b',()=>{it('proto',()=>{expect(Object.create(StructuredDataService.prototype)).toBeTruthy();});});
