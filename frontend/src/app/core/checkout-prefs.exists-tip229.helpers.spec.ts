import { CheckoutPrefsService } from './checkout-prefs.service';

describe('CheckoutPrefsService tip229',()=>{it('proto',()=>{expect(Object.create(CheckoutPrefsService.prototype)).toBeTruthy();});});
