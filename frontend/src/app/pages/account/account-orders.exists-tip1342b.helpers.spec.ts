import { AccountOrdersComponent } from './account-orders.component';

describe('AccountOrdersComponent tip1342b',()=>{it('proto',()=>{expect(Object.create(AccountOrdersComponent.prototype)).toBeTruthy();});});
