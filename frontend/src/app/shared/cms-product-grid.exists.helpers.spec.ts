import { CmsProductGridBlockComponent } from './cms-product-grid-block.component';

describe('CmsProductGrid tip',()=>{it('proto',()=>{expect(Object.create(CmsProductGridBlockComponent.prototype)).toBeTruthy();});});
