import { AccountOverviewComponent } from './account-overview.component';

describe('AccountOverviewComponent tip1083',()=>{it('proto',()=>{expect(Object.create(AccountOverviewComponent.prototype)).toBeTruthy();});});
