import { AdminIpBypassComponent } from './admin-ip-bypass.component';

describe('AdminIpBypassComponent tip1442b',()=>{it('proto',()=>{expect(Object.create(AdminIpBypassComponent.prototype)).toBeTruthy();});});
