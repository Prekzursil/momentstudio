import { LegalConsentModalComponent } from './legal-consent-modal.component';

describe('LegalConsentModalComponent tip27b',()=>{it('proto',()=>{expect(Object.create(LegalConsentModalComponent.prototype)).toBeTruthy();});});
