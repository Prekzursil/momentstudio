import { AccountOrdersComponent } from './account-orders.component';

describe('AccountOrdersComponent tip903',()=>{it('proto',()=>{expect(Object.create(AccountOrdersComponent.prototype)).toBeTruthy();});});
