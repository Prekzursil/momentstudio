import { AccountOverviewComponent } from './account-overview.component';

describe('AccountOverviewComponent tip11',()=>{it('proto',()=>{expect(Object.create(AccountOverviewComponent.prototype)).toBeTruthy();});});
