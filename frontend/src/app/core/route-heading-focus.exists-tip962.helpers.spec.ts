import { RouteHeadingFocusService } from './route-heading-focus.service';

describe('RouteHeadingFocusService tip962',()=>{it('proto',()=>{expect(Object.create(RouteHeadingFocusService.prototype)).toBeTruthy();});});
