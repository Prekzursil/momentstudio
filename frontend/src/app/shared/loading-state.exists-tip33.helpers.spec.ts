import { LoadingStateComponent } from './loading-state.component';

describe('LoadingStateComponent tip33',()=>{it('proto',()=>{expect(Object.create(LoadingStateComponent.prototype)).toBeTruthy();});});
