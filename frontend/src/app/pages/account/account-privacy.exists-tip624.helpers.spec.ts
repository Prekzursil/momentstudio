import { AccountPrivacyComponent } from './account-privacy.component';

describe('AccountPrivacyComponent tip624',()=>{it('proto',()=>{expect(Object.create(AccountPrivacyComponent.prototype)).toBeTruthy();});});
