import { AdminLayoutComponent } from './admin-layout.component';

describe('AdminLayoutComponent tip843',()=>{it('proto',()=>{expect(Object.create(AdminLayoutComponent.prototype)).toBeTruthy();});});
