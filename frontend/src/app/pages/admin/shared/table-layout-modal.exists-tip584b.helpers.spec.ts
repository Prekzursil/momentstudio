import { TableLayoutModalComponent } from './table-layout-modal.component';

describe('TableLayoutModalComponent tip584b',()=>{it('proto',()=>{expect(Object.create(TableLayoutModalComponent.prototype)).toBeTruthy();});});
