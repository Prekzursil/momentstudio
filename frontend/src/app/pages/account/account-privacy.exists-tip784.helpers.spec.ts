import { AccountPrivacyComponent } from './account-privacy.component';

describe('AccountPrivacyComponent tip784',()=>{it('proto',()=>{expect(Object.create(AccountPrivacyComponent.prototype)).toBeTruthy();});});
