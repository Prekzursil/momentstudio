import { LockerPickerComponent } from './locker-picker.component';

describe('LockerPickerComponent tip27',()=>{it('proto',()=>{expect(Object.create(LockerPickerComponent.prototype)).toBeTruthy();});});
