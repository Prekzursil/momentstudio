import { AccountCommentsComponent } from './account-comments.component';

describe('AccountCommentsComponent tip12c',()=>{it('proto',()=>{expect(Object.create(AccountCommentsComponent.prototype)).toBeTruthy();});});
