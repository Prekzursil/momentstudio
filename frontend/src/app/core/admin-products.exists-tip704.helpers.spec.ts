import { AdminProductsService } from './admin-products.service';

describe('AdminProductsService tip704',()=>{it('proto',()=>{expect(Object.create(AdminProductsService.prototype)).toBeTruthy();});});
