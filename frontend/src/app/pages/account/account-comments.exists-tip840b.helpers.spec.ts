import { AccountCommentsComponent } from './account-comments.component';

describe('AccountCommentsComponent tip840b',()=>{it('proto',()=>{expect(Object.create(AccountCommentsComponent.prototype)).toBeTruthy();});});
