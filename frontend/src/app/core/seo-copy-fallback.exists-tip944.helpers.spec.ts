import { SeoCopyFallbackService } from './seo-copy-fallback.service';

describe('SeoCopyFallbackService tip944',()=>{it('proto',()=>{expect(Object.create(SeoCopyFallbackService.prototype)).toBeTruthy();});});
