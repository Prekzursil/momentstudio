import { CustomerTimelineComponent } from './customer-timeline.component';

describe('CustomerTimelineComponent tip1602b',()=>{it('proto',()=>{expect(Object.create(CustomerTimelineComponent.prototype)).toBeTruthy();});});
