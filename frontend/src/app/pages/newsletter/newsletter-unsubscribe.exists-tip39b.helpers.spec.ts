import { NewsletterUnsubscribeComponent } from './newsletter-unsubscribe.component';

describe('NewsletterUnsubscribeComponent tip39b',()=>{it('proto',()=>{expect(Object.create(NewsletterUnsubscribeComponent.prototype)).toBeTruthy();});});
