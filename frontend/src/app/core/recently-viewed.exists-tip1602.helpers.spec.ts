import { RecentlyViewedService } from './recently-viewed.service';

describe('RecentlyViewedService tip1602',()=>{it('proto',()=>{expect(Object.create(RecentlyViewedService.prototype)).toBeTruthy();});});
