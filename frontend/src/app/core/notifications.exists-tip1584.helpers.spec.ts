import { NotificationsService } from './notifications.service';

describe('NotificationsService tip1584',()=>{it('proto',()=>{expect(Object.create(NotificationsService.prototype)).toBeTruthy();});});
