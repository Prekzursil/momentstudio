import { ThemeResetFrameComponent } from './theme-reset-frame.component';

describe('ThemeResetFrameComponent tip14b',()=>{it('proto',()=>{expect(Object.create(ThemeResetFrameComponent.prototype)).toBeTruthy();});});
