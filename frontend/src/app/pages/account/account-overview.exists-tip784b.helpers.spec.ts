import { AccountOverviewComponent } from './account-overview.component';

describe('AccountOverviewComponent tip784b',()=>{it('proto',()=>{expect(Object.create(AccountOverviewComponent.prototype)).toBeTruthy();});});
