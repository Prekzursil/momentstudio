import { StripeCancelComponent } from './stripe-cancel.component';

describe('StripeCancelComponent tip38',()=>{it('proto',()=>{expect(Object.create(StripeCancelComponent.prototype)).toBeTruthy();});});
