import { ProductCardComponent } from './product-card.component';

describe('ProductCardComponent tip11b',()=>{it('proto',()=>{expect(Object.create(ProductCardComponent.prototype)).toBeTruthy();});});
