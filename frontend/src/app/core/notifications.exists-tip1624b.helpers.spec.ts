import { NotificationsService } from './notifications.service';

describe('NotificationsService tip1624b',()=>{it('proto',()=>{expect(Object.create(NotificationsService.prototype)).toBeTruthy();});});
