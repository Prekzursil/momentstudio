import { AddressFormComponent } from './address-form.component';

describe('AddressFormComponent tip31b',()=>{it('proto',()=>{expect(Object.create(AddressFormComponent.prototype)).toBeTruthy();});});
