import { AdminProductsService } from './admin-products.service';

describe('AdminProductsService tip175',()=>{it('proto',()=>{expect(Object.create(AdminProductsService.prototype)).toBeTruthy();});});
