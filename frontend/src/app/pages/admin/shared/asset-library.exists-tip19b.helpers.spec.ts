import { AssetLibraryComponent } from './asset-library.component';

describe('AssetLibraryComponent tip19b',()=>{it('proto',()=>{expect(Object.create(AssetLibraryComponent.prototype)).toBeTruthy();});});
