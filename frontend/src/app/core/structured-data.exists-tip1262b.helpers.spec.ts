import { StructuredDataService } from './structured-data.service';

describe('StructuredDataService tip1262b',()=>{it('proto',()=>{expect(Object.create(StructuredDataService.prototype)).toBeTruthy();});});
