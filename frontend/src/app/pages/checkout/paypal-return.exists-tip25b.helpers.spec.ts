import { PayPalReturnComponent } from './paypal-return.component';

describe('PayPalReturnComponent tip25b',()=>{it('proto',()=>{expect(Object.create(PayPalReturnComponent.prototype)).toBeTruthy();});});
