import { ThemeResetFrameComponent } from './theme-reset-frame.component';

describe('ThemeResetFrameComponent tip33b',()=>{it('proto',()=>{expect(Object.create(ThemeResetFrameComponent.prototype)).toBeTruthy();});});
