import { PayPalReturnComponent } from './paypal-return.component';

describe('PayPalReturnComponent tip14',()=>{it('proto',()=>{expect(Object.create(PayPalReturnComponent.prototype)).toBeTruthy();});});
