import { ProductImageManagerModalComponent } from './product-image-manager-modal.component';

describe('ProductImageManagerModalComponent tip33b',()=>{it('proto',()=>{expect(Object.create(ProductImageManagerModalComponent.prototype)).toBeTruthy();});});
