import { LegalConsentModalComponent } from './legal-consent-modal.component';

describe('LegalConsentModalComponent tip37',()=>{it('proto',()=>{expect(Object.create(LegalConsentModalComponent.prototype)).toBeTruthy();});});
