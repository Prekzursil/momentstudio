import { AccountCouponsComponent } from './account-coupons.component';

describe('AccountCouponsComponent tip15',()=>{it('proto',()=>{expect(Object.create(AccountCouponsComponent.prototype)).toBeTruthy();});});
