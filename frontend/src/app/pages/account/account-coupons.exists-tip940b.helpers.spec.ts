import { AccountCouponsComponent } from './account-coupons.component';

describe('AccountCouponsComponent tip940b',()=>{it('proto',()=>{expect(Object.create(AccountCouponsComponent.prototype)).toBeTruthy();});});
