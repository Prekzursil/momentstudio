import { LegalConsentModalComponent } from './legal-consent-modal.component';

describe('LegalConsentModalComponent tip14b',()=>{it('proto',()=>{expect(Object.create(LegalConsentModalComponent.prototype)).toBeTruthy();});});
