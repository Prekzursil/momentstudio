import { CarouselBlockComponent } from './carousel-block.component';

describe('CarouselBlockComponent tip40',()=>{it('proto',()=>{expect(Object.create(CarouselBlockComponent.prototype)).toBeTruthy();});});
