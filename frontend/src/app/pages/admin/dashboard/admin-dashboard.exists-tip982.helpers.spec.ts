import { AdminDashboardComponent } from './admin-dashboard.component';

describe('AdminDashboardComponent tip982',()=>{it('proto',()=>{expect(Object.create(AdminDashboardComponent.prototype)).toBeTruthy();});});
