import { NewsletterConfirmComponent } from './newsletter-confirm.component';

describe('NewsletterConfirmComponent tip8',()=>{it('proto',()=>{expect(Object.create(NewsletterConfirmComponent.prototype)).toBeTruthy();});});
