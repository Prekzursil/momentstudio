import { GoogleCallbackComponent } from './google-callback.component';

describe('GoogleCallbackComponent tip760',()=>{it('proto',()=>{expect(Object.create(GoogleCallbackComponent.prototype)).toBeTruthy();});});
