import { ProductQuickViewModalComponent } from './product-quick-view-modal.component';

describe('ProductQuickViewModalComponent tip39',()=>{it('proto',()=>{expect(Object.create(ProductQuickViewModalComponent.prototype)).toBeTruthy();});});
