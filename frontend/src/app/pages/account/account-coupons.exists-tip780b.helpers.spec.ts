import { AccountCouponsComponent } from './account-coupons.component';

describe('AccountCouponsComponent tip780b',()=>{it('proto',()=>{expect(Object.create(AccountCouponsComponent.prototype)).toBeTruthy();});});
