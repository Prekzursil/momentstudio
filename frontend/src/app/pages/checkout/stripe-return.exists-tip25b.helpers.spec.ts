import { StripeReturnComponent } from './stripe-return.component';

describe('StripeReturnComponent tip25b',()=>{it('proto',()=>{expect(Object.create(StripeReturnComponent.prototype)).toBeTruthy();});});
