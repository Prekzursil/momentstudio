import { StorefrontAdminModeService } from './storefront-admin-mode.service';

describe('StorefrontAdminModeService tip922',()=>{it('proto',()=>{expect(Object.create(StorefrontAdminModeService.prototype)).toBeTruthy();});});
