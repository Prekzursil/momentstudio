import { AccountSecurityComponent } from './account-security.component';

describe('AccountSecurityComponent tip1640',()=>{it('proto',()=>{expect(Object.create(AccountSecurityComponent.prototype)).toBeTruthy();});});
