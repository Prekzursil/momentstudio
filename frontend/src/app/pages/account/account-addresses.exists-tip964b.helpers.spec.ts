import { AccountAddressesComponent } from './account-addresses.component';

describe('AccountAddressesComponent tip964b',()=>{it('proto',()=>{expect(Object.create(AccountAddressesComponent.prototype)).toBeTruthy();});});
