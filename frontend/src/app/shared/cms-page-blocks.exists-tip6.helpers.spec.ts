import { CmsPageBlocksComponent } from './cms-page-blocks.component';

describe('CmsPageBlocksComponent tip6',()=>{it('proto',()=>{expect(Object.create(CmsPageBlocksComponent.prototype)).toBeTruthy();});});
