import { AdminCouponsComponent } from './admin-coupons.component';

describe('AdminCouponsComponent tip781',()=>{it('proto',()=>{expect(Object.create(AdminCouponsComponent.prototype)).toBeTruthy();});});
