import { AccountNotificationsInboxComponent } from './account-notifications-inbox.component';

describe('AccountNotificationsInboxComponent tip1421',()=>{it('proto',()=>{expect(Object.create(AccountNotificationsInboxComponent.prototype)).toBeTruthy();});});
