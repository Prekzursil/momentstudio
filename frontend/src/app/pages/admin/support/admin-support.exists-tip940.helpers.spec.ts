import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip940',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
