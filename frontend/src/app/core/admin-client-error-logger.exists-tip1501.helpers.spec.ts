import { AdminClientErrorLoggerService } from './admin-client-error-logger.service';

describe('AdminClientErrorLoggerService tip1501',()=>{it('proto',()=>{expect(Object.create(AdminClientErrorLoggerService.prototype)).toBeTruthy();});});
