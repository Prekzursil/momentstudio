import { CustomerTimelineComponent } from './customer-timeline.component';

describe('CustomerTimelineComponent tip642b',()=>{it('proto',()=>{expect(Object.create(CustomerTimelineComponent.prototype)).toBeTruthy();});});
