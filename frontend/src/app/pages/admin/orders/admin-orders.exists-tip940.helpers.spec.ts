import { AdminOrdersComponent } from './admin-orders.component';

describe('AdminOrdersComponent tip940',()=>{it('proto',()=>{expect(Object.create(AdminOrdersComponent.prototype)).toBeTruthy();});});
