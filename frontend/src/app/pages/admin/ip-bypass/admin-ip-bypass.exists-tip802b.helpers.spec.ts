import { AdminIpBypassComponent } from './admin-ip-bypass.component';

describe('AdminIpBypassComponent tip802b',()=>{it('proto',()=>{expect(Object.create(AdminIpBypassComponent.prototype)).toBeTruthy();});});
