import { AccountOrdersComponent } from './account-orders.component';

describe('AccountOrdersComponent tip1462b',()=>{it('proto',()=>{expect(Object.create(AccountOrdersComponent.prototype)).toBeTruthy();});});
