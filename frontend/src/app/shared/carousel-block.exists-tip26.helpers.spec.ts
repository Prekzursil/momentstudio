import { CarouselBlockComponent } from './carousel-block.component';

describe('CarouselBlockComponent tip26',()=>{it('proto',()=>{expect(Object.create(CarouselBlockComponent.prototype)).toBeTruthy();});});
