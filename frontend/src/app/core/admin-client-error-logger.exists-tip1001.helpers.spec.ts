import { AdminClientErrorLoggerService } from './admin-client-error-logger.service';

describe('AdminClientErrorLoggerService tip1001',()=>{it('proto',()=>{expect(Object.create(AdminClientErrorLoggerService.prototype)).toBeTruthy();});});
