import { AccountPrivacyComponent } from './account-privacy.component';

describe('AccountPrivacyComponent tip584',()=>{it('proto',()=>{expect(Object.create(AccountPrivacyComponent.prototype)).toBeTruthy();});});
