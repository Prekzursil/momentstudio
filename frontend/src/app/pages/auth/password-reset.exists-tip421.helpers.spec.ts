import { PasswordResetComponent } from './password-reset.component';

describe('PasswordResetComponent tip421',()=>{it('proto',()=>{expect(Object.create(PasswordResetComponent.prototype)).toBeTruthy();});});
