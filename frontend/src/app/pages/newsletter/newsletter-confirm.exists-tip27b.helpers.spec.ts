import { NewsletterConfirmComponent } from './newsletter-confirm.component';

describe('NewsletterConfirmComponent tip27b',()=>{it('proto',()=>{expect(Object.create(NewsletterConfirmComponent.prototype)).toBeTruthy();});});
