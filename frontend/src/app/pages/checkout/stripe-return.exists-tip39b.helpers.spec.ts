import { StripeReturnComponent } from './stripe-return.component';

describe('StripeReturnComponent tip39b',()=>{it('proto',()=>{expect(Object.create(StripeReturnComponent.prototype)).toBeTruthy();});});
