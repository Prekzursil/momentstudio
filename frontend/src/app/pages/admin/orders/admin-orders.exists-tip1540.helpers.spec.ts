import { AdminOrdersComponent } from './admin-orders.component';

describe('AdminOrdersComponent tip1540',()=>{it('proto',()=>{expect(Object.create(AdminOrdersComponent.prototype)).toBeTruthy();});});
