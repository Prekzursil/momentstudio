import { VerifyEmailComponent } from './verify-email.component';

describe('VerifyEmailComponent tip864',()=>{it('proto',()=>{expect(Object.create(VerifyEmailComponent.prototype)).toBeTruthy();});});
