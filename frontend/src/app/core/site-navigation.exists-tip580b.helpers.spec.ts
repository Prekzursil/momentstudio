import { SiteNavigationService } from './site-navigation.service';

describe('SiteNavigationService tip580b',()=>{it('proto',()=>{expect(Object.create(SiteNavigationService.prototype)).toBeTruthy();});});
