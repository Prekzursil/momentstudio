import { AdminIpBypassComponent } from './admin-ip-bypass.component';

describe('AdminIpBypassComponent tip1122b',()=>{it('proto',()=>{expect(Object.create(AdminIpBypassComponent.prototype)).toBeTruthy();});});
