import { CmsEditorPrefsService } from './cms-editor-prefs.service';

describe('CmsEditorPrefsService tip441',()=>{it('proto',()=>{expect(Object.create(CmsEditorPrefsService.prototype)).toBeTruthy();});});
