import { VerifyEmailComponent } from './verify-email.component';

describe('VerifyEmailComponent tip1523',()=>{it('proto',()=>{expect(Object.create(VerifyEmailComponent.prototype)).toBeTruthy();});});
