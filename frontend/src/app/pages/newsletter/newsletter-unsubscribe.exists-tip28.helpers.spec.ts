import { NewsletterUnsubscribeComponent } from './newsletter-unsubscribe.component';

describe('NewsletterUnsubscribeComponent tip28',()=>{it('proto',()=>{expect(Object.create(NewsletterUnsubscribeComponent.prototype)).toBeTruthy();});});
