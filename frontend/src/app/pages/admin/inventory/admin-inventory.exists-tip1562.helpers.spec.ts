import { AdminInventoryComponent } from './admin-inventory.component';

describe('AdminInventoryComponent tip1562',()=>{it('proto',()=>{expect(Object.create(AdminInventoryComponent.prototype)).toBeTruthy();});});
