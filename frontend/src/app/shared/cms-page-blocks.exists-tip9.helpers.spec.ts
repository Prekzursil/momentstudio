import { CmsPageBlocksComponent } from './cms-page-blocks.component';

describe('CmsPageBlocksComponent tip9',()=>{it('proto',()=>{expect(Object.create(CmsPageBlocksComponent.prototype)).toBeTruthy();});});
