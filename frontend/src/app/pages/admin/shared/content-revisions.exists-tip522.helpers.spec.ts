import { ContentRevisionsComponent } from './content-revisions.component';

describe('ContentRevisionsComponent tip522',()=>{it('proto',()=>{expect(Object.create(ContentRevisionsComponent.prototype)).toBeTruthy();});});
