import { AdminContentSchedulingComponent } from './admin-content-scheduling.component';

describe('AdminContentSchedulingComponent tip720',()=>{it('proto',()=>{expect(Object.create(AdminContentSchedulingComponent.prototype)).toBeTruthy();});});
