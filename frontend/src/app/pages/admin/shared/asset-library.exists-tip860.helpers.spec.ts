import { AssetLibraryComponent } from './asset-library.component';

describe('AssetLibraryComponent tip860',()=>{it('proto',()=>{expect(Object.create(AssetLibraryComponent.prototype)).toBeTruthy();});});
