import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip1520',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
