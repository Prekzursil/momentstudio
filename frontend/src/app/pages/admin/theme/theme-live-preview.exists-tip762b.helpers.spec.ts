import { ThemeLivePreviewComponent } from './theme-live-preview.component';

describe('ThemeLivePreviewComponent tip762b',()=>{it('proto',()=>{expect(Object.create(ThemeLivePreviewComponent.prototype)).toBeTruthy();});});
