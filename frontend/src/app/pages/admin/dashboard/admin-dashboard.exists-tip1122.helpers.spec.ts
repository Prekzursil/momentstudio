import { AdminDashboardComponent } from './admin-dashboard.component';

describe('AdminDashboardComponent tip1122',()=>{it('proto',()=>{expect(Object.create(AdminDashboardComponent.prototype)).toBeTruthy();});});
