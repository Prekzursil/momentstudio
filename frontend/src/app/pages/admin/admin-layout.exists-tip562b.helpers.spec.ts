import { AdminLayoutComponent } from './admin-layout.component';

describe('AdminLayoutComponent tip562b',()=>{it('proto',()=>{expect(Object.create(AdminLayoutComponent.prototype)).toBeTruthy();});});
