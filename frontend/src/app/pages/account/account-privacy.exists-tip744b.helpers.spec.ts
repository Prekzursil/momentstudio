import { AccountPrivacyComponent } from './account-privacy.component';

describe('AccountPrivacyComponent tip744b',()=>{it('proto',()=>{expect(Object.create(AccountPrivacyComponent.prototype)).toBeTruthy();});});
