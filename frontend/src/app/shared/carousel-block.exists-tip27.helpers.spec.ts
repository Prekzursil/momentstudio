import { CarouselBlockComponent } from './carousel-block.component';

describe('CarouselBlockComponent tip27',()=>{it('proto',()=>{expect(Object.create(CarouselBlockComponent.prototype)).toBeTruthy();});});
