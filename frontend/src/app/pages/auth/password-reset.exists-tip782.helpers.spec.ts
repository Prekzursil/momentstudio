import { PasswordResetComponent } from './password-reset.component';

describe('PasswordResetComponent tip782',()=>{it('proto',()=>{expect(Object.create(PasswordResetComponent.prototype)).toBeTruthy();});});
