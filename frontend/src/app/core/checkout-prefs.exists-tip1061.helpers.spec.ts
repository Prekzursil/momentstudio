import { CheckoutPrefsService } from './checkout-prefs.service';

describe('CheckoutPrefsService tip1061',()=>{it('proto',()=>{expect(Object.create(CheckoutPrefsService.prototype)).toBeTruthy();});});
