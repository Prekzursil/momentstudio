import { ProductCardComponent } from './product-card.component';

describe('ProductCardComponent tip12b',()=>{it('proto',()=>{expect(Object.create(ProductCardComponent.prototype)).toBeTruthy();});});
