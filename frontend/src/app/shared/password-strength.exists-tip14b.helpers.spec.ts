import { PasswordStrengthComponent } from './password-strength.component';

describe('PasswordStrengthComponent tip14b',()=>{it('proto',()=>{expect(Object.create(PasswordStrengthComponent.prototype)).toBeTruthy();});});
