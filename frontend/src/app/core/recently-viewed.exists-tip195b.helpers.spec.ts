import { RecentlyViewedService } from './recently-viewed.service';

describe('RecentlyViewedService tip195b',()=>{it('proto',()=>{expect(Object.create(RecentlyViewedService.prototype)).toBeTruthy();});});
