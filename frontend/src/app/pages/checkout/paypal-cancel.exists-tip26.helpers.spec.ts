import { PayPalCancelComponent } from './paypal-cancel.component';

describe('PayPalCancelComponent tip26',()=>{it('proto',()=>{expect(Object.create(PayPalCancelComponent.prototype)).toBeTruthy();});});
