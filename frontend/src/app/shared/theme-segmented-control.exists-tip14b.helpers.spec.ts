import { ThemeSegmentedControlComponent } from './theme-segmented-control.component';

describe('ThemeSegmentedControlComponent tip14b',()=>{it('proto',()=>{expect(Object.create(ThemeSegmentedControlComponent.prototype)).toBeTruthy();});});
