import { AccountOrdersComponent } from './account-orders.component';

describe('AccountOrdersComponent tip842b',()=>{it('proto',()=>{expect(Object.create(AccountOrdersComponent.prototype)).toBeTruthy();});});
