import { PayPalReturnComponent } from './paypal-return.component';

describe('PayPalReturnComponent tip43b',()=>{it('proto',()=>{expect(Object.create(PayPalReturnComponent.prototype)).toBeTruthy();});});
