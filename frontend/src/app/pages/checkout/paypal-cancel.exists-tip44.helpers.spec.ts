import { PayPalCancelComponent } from './paypal-cancel.component';

describe('PayPalCancelComponent tip44',()=>{it('proto',()=>{expect(Object.create(PayPalCancelComponent.prototype)).toBeTruthy();});});
