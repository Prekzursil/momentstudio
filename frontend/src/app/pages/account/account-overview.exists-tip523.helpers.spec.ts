import { AccountOverviewComponent } from './account-overview.component';

describe('AccountOverviewComponent tip523',()=>{it('proto',()=>{expect(Object.create(AccountOverviewComponent.prototype)).toBeTruthy();});});
