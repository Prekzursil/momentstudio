import { AdminOrdersComponent } from './admin-orders.component';

describe('AdminOrdersComponent tip1280',()=>{it('proto',()=>{expect(Object.create(AdminOrdersComponent.prototype)).toBeTruthy();});});
