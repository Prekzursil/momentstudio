import { AccountWishlistComponent } from './account-wishlist.component';

describe('AccountWishlistComponent tip781',()=>{it('proto',()=>{expect(Object.create(AccountWishlistComponent.prototype)).toBeTruthy();});});
