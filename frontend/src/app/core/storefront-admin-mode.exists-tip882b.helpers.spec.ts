import { StorefrontAdminModeService } from './storefront-admin-mode.service';

describe('StorefrontAdminModeService tip882b',()=>{it('proto',()=>{expect(Object.create(StorefrontAdminModeService.prototype)).toBeTruthy();});});
