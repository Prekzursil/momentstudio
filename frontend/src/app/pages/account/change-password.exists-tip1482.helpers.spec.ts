import { ChangePasswordComponent } from './change-password.component';

describe('ChangePasswordComponent tip1482',()=>{it('proto',()=>{expect(Object.create(ChangePasswordComponent.prototype)).toBeTruthy();});});
