import { ThemeLivePreviewComponent } from './theme-live-preview.component';

describe('ThemeLivePreviewComponent tip442',()=>{it('proto',()=>{expect(Object.create(ThemeLivePreviewComponent.prototype)).toBeTruthy();});});
