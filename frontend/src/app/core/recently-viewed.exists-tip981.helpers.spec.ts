import { RecentlyViewedService } from './recently-viewed.service';

describe('RecentlyViewedService tip981',()=>{it('proto',()=>{expect(Object.create(RecentlyViewedService.prototype)).toBeTruthy();});});
