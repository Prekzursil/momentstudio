import { ChangePasswordComponent } from './change-password.component';

describe('ChangePasswordComponent tip763',()=>{it('proto',()=>{expect(Object.create(ChangePasswordComponent.prototype)).toBeTruthy();});});
