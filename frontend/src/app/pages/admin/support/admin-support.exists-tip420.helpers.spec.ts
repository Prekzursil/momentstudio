import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip420',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
