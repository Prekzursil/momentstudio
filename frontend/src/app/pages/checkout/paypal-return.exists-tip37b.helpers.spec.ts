import { PayPalReturnComponent } from './paypal-return.component';

describe('PayPalReturnComponent tip37b',()=>{it('proto',()=>{expect(Object.create(PayPalReturnComponent.prototype)).toBeTruthy();});});
