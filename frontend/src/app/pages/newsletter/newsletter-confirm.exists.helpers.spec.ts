import { NewsletterConfirmComponent } from './newsletter-confirm.component';

describe('NewsletterConfirmComponent tip',()=>{it('proto',()=>{expect(Object.create(NewsletterConfirmComponent.prototype)).toBeTruthy();});});
