import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip800',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
