import { AssetLibraryComponent } from './asset-library.component';

describe('AssetLibraryComponent tip640',()=>{it('proto',()=>{expect(Object.create(AssetLibraryComponent.prototype)).toBeTruthy();});});
