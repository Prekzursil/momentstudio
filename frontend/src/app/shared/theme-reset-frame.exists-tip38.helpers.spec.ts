import { ThemeResetFrameComponent } from './theme-reset-frame.component';

describe('ThemeResetFrameComponent tip38',()=>{it('proto',()=>{expect(Object.create(ThemeResetFrameComponent.prototype)).toBeTruthy();});});
