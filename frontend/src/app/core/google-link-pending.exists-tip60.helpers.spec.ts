import { GoogleLinkPendingService } from './google-link-pending.service';

describe('GoogleLinkPendingService tip60',()=>{it('proto',()=>{expect(Object.create(GoogleLinkPendingService.prototype)).toBeTruthy();});});
