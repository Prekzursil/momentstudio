import { AccountAddressesComponent } from './account-addresses.component';

describe('AccountAddressesComponent tip19b',()=>{it('proto',()=>{expect(Object.create(AccountAddressesComponent.prototype)).toBeTruthy();});});
