import { PasswordStrengthComponent } from './password-strength.component';

describe('PasswordStrengthComponent tip37',()=>{it('proto',()=>{expect(Object.create(PasswordStrengthComponent.prototype)).toBeTruthy();});});
