import { InlineErrorCardComponent } from './inline-error-card.component';

describe('InlineError tip',()=>{it('proto',()=>{expect(Object.create(InlineErrorCardComponent.prototype)).toBeTruthy();});});
