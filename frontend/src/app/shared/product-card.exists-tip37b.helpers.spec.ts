import { ProductCardComponent } from './product-card.component';

describe('ProductCardComponent tip37b',()=>{it('proto',()=>{expect(Object.create(ProductCardComponent.prototype)).toBeTruthy();});});
