import { AdminContentSchedulingComponent } from './admin-content-scheduling.component';

describe('AdminContentSchedulingComponent tip1520',()=>{it('proto',()=>{expect(Object.create(AdminContentSchedulingComponent.prototype)).toBeTruthy();});});
