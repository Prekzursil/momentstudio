import { AdminIpBypassComponent } from './admin-ip-bypass.component';

describe('AdminIpBypassComponent tip962b',()=>{it('proto',()=>{expect(Object.create(AdminIpBypassComponent.prototype)).toBeTruthy();});});
