import { CmsEditorPrefsService } from './cms-editor-prefs.service';

describe('CmsEditorPrefsService tip1561',()=>{it('proto',()=>{expect(Object.create(CmsEditorPrefsService.prototype)).toBeTruthy();});});
