import { StorefrontAdminModeService } from './storefront-admin-mode.service';

describe('StorefrontAdminModeService tip10b',()=>{it('proto',()=>{expect(Object.create(StorefrontAdminModeService.prototype)).toBeTruthy();});});
