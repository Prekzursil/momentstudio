import { AccountSecurityComponent } from './account-security.component';

describe('AccountSecurityComponent tip880',()=>{it('proto',()=>{expect(Object.create(AccountSecurityComponent.prototype)).toBeTruthy();});});
