import { AdminFavoritesService } from './admin-favorites.service';

describe('AdminFavoritesService tip862b',()=>{it('proto',()=>{expect(Object.create(AdminFavoritesService.prototype)).toBeTruthy();});});
