import { AdminOrderDetailComponent } from './admin-order-detail.component';

describe('AdminOrderDetailComponent tip884',()=>{it('proto',()=>{expect(Object.create(AdminOrderDetailComponent.prototype)).toBeTruthy();});});
