import { AdminInventoryComponent } from './admin-inventory.component';

describe('AdminInventoryComponent tip662',()=>{it('proto',()=>{expect(Object.create(AdminInventoryComponent.prototype)).toBeTruthy();});});
