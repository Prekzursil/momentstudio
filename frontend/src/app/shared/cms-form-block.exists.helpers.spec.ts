import { CmsFormBlockComponent } from './cms-form-block.component';

describe('CmsFormBlockComponent tip',()=>{it('proto',()=>{expect(Object.create(CmsFormBlockComponent.prototype)).toBeTruthy();});});
