import { AccountWishlistComponent } from './account-wishlist.component';

describe('AccountWishlistComponent tip540b',()=>{it('proto',()=>{expect(Object.create(AccountWishlistComponent.prototype)).toBeTruthy();});});
