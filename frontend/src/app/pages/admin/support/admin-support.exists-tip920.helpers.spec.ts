import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip920',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
