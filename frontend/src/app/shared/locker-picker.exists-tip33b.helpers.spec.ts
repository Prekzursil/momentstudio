import { LockerPickerComponent } from './locker-picker.component';

describe('LockerPickerComponent tip33b',()=>{it('proto',()=>{expect(Object.create(LockerPickerComponent.prototype)).toBeTruthy();});});
