import { PasswordResetComponent } from './password-reset.component';

describe('PasswordResetComponent tip1101',()=>{it('proto',()=>{expect(Object.create(PasswordResetComponent.prototype)).toBeTruthy();});});
