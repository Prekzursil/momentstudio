import { AdminInventoryComponent } from './admin-inventory.component';

describe('AdminInventoryComponent tip882',()=>{it('proto',()=>{expect(Object.create(AdminInventoryComponent.prototype)).toBeTruthy();});});
