import { AdminSupportComponent } from './admin-support.component';

describe('AdminSupportComponent tip600',()=>{it('proto',()=>{expect(Object.create(AdminSupportComponent.prototype)).toBeTruthy();});});
