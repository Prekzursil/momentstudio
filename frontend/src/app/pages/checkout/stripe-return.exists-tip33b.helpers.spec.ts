import { StripeReturnComponent } from './stripe-return.component';

describe('StripeReturnComponent tip33b',()=>{it('proto',()=>{expect(Object.create(StripeReturnComponent.prototype)).toBeTruthy();});});
