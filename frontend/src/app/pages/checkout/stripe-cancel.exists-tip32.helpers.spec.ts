import { StripeCancelComponent } from './stripe-cancel.component';

describe('StripeCancelComponent tip32',()=>{it('proto',()=>{expect(Object.create(StripeCancelComponent.prototype)).toBeTruthy();});});
