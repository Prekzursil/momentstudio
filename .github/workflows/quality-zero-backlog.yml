name: Quality Zero Backlog

on:
  schedule:
    - cron: "0 3 * * *"

jobs:
  backlog-sweep:
    uses: Prekzursil/quality-zero-platform/.github/workflows/reusable-backlog-sweep.yml@37a6db2c75dc6891eea39007c96e8f9cbde22579 # main
    with:
      repo_slug: ${{ github.repository }}
      lane: coverage
