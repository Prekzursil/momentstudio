import { ChangePasswordComponent } from './change-password.component';

describe('ChangePasswordComponent tip502',()=>{it('proto',()=>{expect(Object.create(ChangePasswordComponent.prototype)).toBeTruthy();});});
