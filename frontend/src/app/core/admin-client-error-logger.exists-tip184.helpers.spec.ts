import { AdminClientErrorLoggerService } from './admin-client-error-logger.service';

describe('AdminClientErrorLoggerService tip184',()=>{it('proto',()=>{expect(Object.create(AdminClientErrorLoggerService.prototype)).toBeTruthy();});});
