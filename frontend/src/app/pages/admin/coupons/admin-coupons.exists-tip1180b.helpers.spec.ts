import { AdminCouponsComponent } from './admin-coupons.component';

describe('AdminCouponsComponent tip1180b',()=>{it('proto',()=>{expect(Object.create(AdminCouponsComponent.prototype)).toBeTruthy();});});
