import { PayPalReturnComponent } from './paypal-return.component';

describe('PayPalReturnComponent tip31b',()=>{it('proto',()=>{expect(Object.create(PayPalReturnComponent.prototype)).toBeTruthy();});});
