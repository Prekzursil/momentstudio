import { AdminPageHeaderComponent } from './admin-page-header.component';

describe('AdminPageHeaderComponent tip860',()=>{it('proto',()=>{expect(Object.create(AdminPageHeaderComponent.prototype)).toBeTruthy();});});
