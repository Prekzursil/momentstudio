import { RouteHeadingFocusService } from './route-heading-focus.service';

describe('RouteHeadingFocusService tip842b',()=>{it('proto',()=>{expect(Object.create(RouteHeadingFocusService.prototype)).toBeTruthy();});});
