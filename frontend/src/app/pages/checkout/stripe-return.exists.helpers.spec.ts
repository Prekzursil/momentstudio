import { StripeReturnComponent } from './stripe-return.component';

describe('StripeReturnComponent tip',()=>{it('proto',()=>{expect(Object.create(StripeReturnComponent.prototype)).toBeTruthy();});});
