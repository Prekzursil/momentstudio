interface:
  display_name: "Momentstudio"
  short_description: "Repo-specific patterns and workflows for momentstudio"
  default_prompt: "Use the momentstudio repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
